# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# For a description of this file, see:
#   http://www.postgresql.org/docs/9.4/static/auth-pg-hba-conf.html
#
# For a full version of this file with comments, see:
#   http://git.postgresql.org/gitweb/?p=postgresql.git;a=blob_plain;f=src/backend/libpq/pg_hba.conf.sample;hb=REL9_4_STABLE

# Allow users to connect over the local socket
local   all              all                               trust

# Allow users to connect from the local host with valid credentials
host    samerole         all           samehost            md5

# Allow users to connect over the local network with valid credentials
host    samerole         all           10.0.0.0/8           md5
host    samerole         all           172.16.0.0/12        md5
host    samerole         all           192.168.0.0/16       md5
